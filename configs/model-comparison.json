{
  "version": "mlp-dnn-cnn-500-v2",
  "dataset": "local-results/qmeter-500-v1/broad.npz",
  "dataset_sha256": "af70281eb8bd6efd52dcfe3b764d2bc12712259207342b937bb9e9f2aa157c37",
  "run_directory": "local-results/model-comparison-500-v2",
  "architectures": [
    "mlp",
    "dnn",
    "physics_multiscale"
  ],
  "training": {
    "epochs": 80,
    "patience": 12,
    "batch_size": 128,
    "learning_rate": 0.0001,
    "seed": 42,
    "batch_order_seed": 42,
    "threads": 2,
    "defer_test": true
  },
  "selection": "Choose each checkpoint by validation MSE. Fix the comparison before evaluating the saved test rows.",
  "scope": "One seed and a common epoch budget on the broad 500-bin TE-area benchmark. The CNN includes a train-only ridge estimate from physical summaries; this is a comparison of complete estimators."
}
